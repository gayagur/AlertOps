# Frontend environment variables
# These are SAFE to expose — prefixed with VITE_ they are bundled into the client

# API base URL — leave empty for local dev (uses Vite proxy)
# Set to your deployed backend URL for production (e.g., https://market-intel-api.onrender.com)
VITE_API_BASE_URL=
